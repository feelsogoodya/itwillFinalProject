<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwillbs.mappers.selllistMapper">


	<select id="getSellList"
		resultType="com.itwillbs.domain.SellListDTO">
		select * from selllist where sellmem_id= #{sellmemId}
		order by sell_date desc
	</select>

  <select id="getSellCount" resultType="java.lang.Integer">
  	select count(*) from selllist
  </select>

</mapper>